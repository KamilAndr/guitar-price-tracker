services:
  postgres:
    image: postgres:16
    container_name: guitar_tracker_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: tracker
      POSTGRES_PASSWORD: tracker_local
      POSTGRES_DB: guitar_tracker
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data: